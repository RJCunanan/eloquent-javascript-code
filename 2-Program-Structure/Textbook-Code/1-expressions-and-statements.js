// Expressions and Statements

/*
"expression" - a fragment of code that produces a value

If an expression correspons to a sentence fragment, a JS "statement"
corresponds to a full sentence. A program is a list of statements.
*/

1;
!false;

/*
A statment stands on its own, so it amounts to something only if
it affects the world.

Examples: 
- display something on the screen
- change internal state of the machin that will affect the statements
that come after it

These changes are called "side effects"
*/